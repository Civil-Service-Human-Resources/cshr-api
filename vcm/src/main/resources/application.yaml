spring:
  datasource:
    url: jdbc:postgresql://db:5432/cshr
    username: postgres
    password: qwerty
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate.ddl-auto: validate # validate schema on run.  Updates handled by Liquibase.
  location:
    service:
      url: http://location-service.dev.cshr-gov.uk/findlocation/{searchTerm} # will be overridden by an env variable
server:
  port: 8080
