spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cshrtest
    username: postgres
    password: qwerty
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate.ddl-auto: drop # validate schema on run.  Updates handled by Liquibase.
    properties:
        hibernate:
            search:
                default:
                    directory_provider: filesystem
                    indexBase: indexpath
  location:
    service:
      url: http://location-service.dev.cshr-gov.uk/findlocation/{searchTerm} # will be overridden by an env variable
server:
  port: 8080
liquibase:
    change-log: classpath:/changelog.xml
