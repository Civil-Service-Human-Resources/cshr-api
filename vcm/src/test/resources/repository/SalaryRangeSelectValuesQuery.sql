SELECT * FROM vacancies WHERE public_opening_date IS NOT NULL AND public_opening_date <= :now AND (point(:searchFromLongitudeValue, :searchFromLatitudeValue) <@> point(longitude, latitude)) < :distance AND salary_max >= :salary_min