SELECT * FROM vacancies WHERE public_opening_date IS NOT NULL AND public_opening_date <= :now AND (point(:searchFromLongitudeValue, :searchFromLatitudeValue) <@> point(longitude, latitude)) < :distance AND CONCAT(title, ' ', description) ILIKE :keyword AND dept_id in (:dept0,:dept1)